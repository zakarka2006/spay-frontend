# Dependencies and build artifacts
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Local build outputs (image builds its own)
dist

# VCS
.git
.gitignore

# Docker files themselves (not needed inside build context)
Dockerfile
docker-compose.yml

# IDE/editor
.vscode
.idea
*.swp
.DS_Store
